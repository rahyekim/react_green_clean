public class today {
    

}


/*
1. char vs varchar
char = 고정 길이
varchar = 가변 길이
2. 배열 sizeof
int students[20];
sizeof(students)

→ 20 × 4 = 80 (int = 4바이트라서)

3. int 기준으로 계산하는 이유
C에서 sizeof(int) 기준으로 메모리 계산
4. 포인터 int *students
“int를 가리키는 주소 변수”
값이 아니라 “주소 저장”
5. calloc
students = calloc(n, sizeof(int));
n개 * int 크기만큼 메모리 할당
0으로 초기화
6. free(students)
malloc/calloc으로 빌린 메모리 반환
포인터 자체가 아니라 “가리키는 메모리” 해제
7. malloc / calloc / realloc
malloc: 그냥 할당
calloc: 0 초기화 포함 할당
realloc: 크기 변경
8. 동적 메모리 개념
실행 중에 필요한 만큼 메모리 빌리는 것
9. int * 의미
“int 값이 있는 위치(주소)를 저장하는 변수 타입”
10. sizeof가 함수에서 다르게 보이는 이유
배열 → 함수 전달 시 포인터로 변환됨 (decay)
11. 배열이 포인터로 바뀌는 이유
함수에서는 배열 전체 복사 안 하고 주소만 전달
12. -> 화살표 연산자
p->g

→ (*p).g의 축약형

13. 구조체 포인터 흐름
p->next
현재 구조체에서 다음 구조체 주소로 이동
14. p->g + (p->i - 1)
문자열 시작 주소 + offset 이동
문자열에서 특정 위치부터 출력

1) 자바 기본 배열(int[]): 방 크기가 딱딱하게 고정된 대신, 컴퓨터가 읽는 속도가 가장 빠르고 최적화되어 있다.
(2) java.util.ArrayList: 기본 배열과 달리, 데이터가 들어오는 대로 방 크기가 자동으로 늘어나는 동적 배열이다.
(3) java.util.Arrays: 고정된 기본 배열을 정렬하거나 출력할 때 쓰는 자바의 편리한 도구 상자(import 필요)이다.
(4) enum (열거형): 단순히 숫자에 이름을 붙인 게 아니라, 관련 상수들을 안전하게 묶어둔 완전한 기능의 클래스이다.
(5) enum 필드 정의: 각 상수가 가질 정보 주머니(변수)이며, 데이터가 변하지 않게 private final로 안전하게 보호한다.
(6) enum 생성자: 외부에서 new로 엉뚱한 마크를 찍어내지 못하도록 무조건 private으로 막혀있다.
(7) 타입 안정성: 컴파일할 때 정해진 enum 값만 들어오도록 원천 차단하여 프로그램의 에러를 방지한다.
(8) Arrays.stream(): enum의 모든 상수 배열을 컨베이어 벨트(레일) 위에 올려놓고 하나씩 처리하는 자바의 현대적 기능이다.
(9) .filter(): 레일 위를 지나가는 enum 상수 중 조건에 맞는 진짜 정품 마크만 골라내는 필터이다.
(10) .equalsIgnoreCase(): 글자를 비교할 때 대소문자 구별 없이 이름이 똑같은지 검사해 주는 편리한 기능이다.
(11)  정적 팩토리 메서드 from(): 외부에서 들어온 단순한 글자(String)를 우리 창고에 있는 정품 enum 객체로 변환해 주는 기계이다.
(12) .orElseThrow(): 변환 기계에 엉뚱한 글자가 들어와서 필터를 통과한 마크가 없을 때, 명확한 에러 메시지를 던지며 안전하게 멈춘다.
(13) interface (계약서): 메서드의 이름과 반환 타입만 있고 실제 내용({})은 없는 껍데기(추상 메서드) 설계도이다.
(14) implements (구현): 인터페이스 계약서에 서명한 클래스는, 그 안의 껍데기 메서드 로직을 무조건 똑같은 이름으로 직접 구현해야만 컴파일 오류가 나지 않는다.
*/